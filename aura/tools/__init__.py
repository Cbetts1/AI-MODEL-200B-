"""aura.tools — built-in tools and the tool registry sub-package.

Modules
-------
registry    : Discovers and stores Tool objects; exposes call-by-name API.
shell       : Executes shell commands safely.
file_ops    : Read / write / list files on disk.
web_search  : Performs a simple web search (DuckDuckGo or configurable).
apk_builder : Orchestrates an Android APK build pipeline.
"""
