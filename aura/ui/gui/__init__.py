"""aura.ui.gui — Web-based GUI for AURA.

The GUI package provides a full web chat interface served directly from
the AURA API server.  No build step, no npm, no CDN — just Python.

Modules
-------
  webui : Self-contained HTML/CSS/JS chat interface with:
          - Responsive chat bubbles with typing indicators
          - Dark / light theme toggle
          - Video call, voice call, screen share buttons (WebRTC-ready)
          - Pre-fab template selector sidebar
          - PWA-installable (service worker + manifest)
          - Keyboard shortcuts (Enter to send, Shift+Enter for newline)
          - Friendly animated AURA branding
"""
