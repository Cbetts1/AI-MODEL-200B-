"""aura.ui.gui — placeholder for a future GUI (web or Qt).

This package is intentionally empty at v0.1.
To add a GUI backend:
  1. Create a module here (e.g. `webui.py` using FastAPI + htmx).
  2. Register it as a CLI sub-command in `aura.ui.cli`.
"""
