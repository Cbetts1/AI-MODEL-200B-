"""aura.core — core engine sub-package.

Modules
-------
engine      : Top-level AuraEngine that ties everything together.
session     : Per-conversation state (history, user preferences).
memory      : Short-term (in-process) and long-term (file-backed) memory.
dispatcher  : Routes an incoming message to the right handler or tool.
"""
