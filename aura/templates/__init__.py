"""aura.templates — Pre-fab solution templates for common use cases.

Templates give users instant access to specialized AURA configurations
without any setup.  Each template defines a system prompt overlay, suggested
tools, and a friendly greeting so AURA can immediately act as a code helper,
writing assistant, math tutor, health advisor, and more.

Usage (in chat)
---------------
    /template list               Show all available templates.
    /template use code_helper    Activate the Code Helper template.
    /template reset              Return to the default AURA persona.
"""
